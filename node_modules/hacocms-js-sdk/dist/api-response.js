/**
 * typed list API response
 */
export class ListApiResponse {
    constructor(json, Constructor) {
        this.meta = json.meta;
        this.data = json.data.map((content) => new Constructor(content));
    }
}
